/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

// create a function
// return value: this function does not return anything
// object[key] = value;
// return whatever is in the object literal.
