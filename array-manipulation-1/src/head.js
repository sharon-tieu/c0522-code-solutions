/* exported head */
function head(array) {
  var firstItem = array[0];
  return firstItem;
}

// create a function to return the first element of the array
// first element of the array is at index 0. This is written as array[0];
// to return the first element of the array, assign it to a new variable and return it.
// var firstItem = array[0];
// then return.
