/* exported last */
function last(array) {
  var lastItem = array[array.length - 1];
  return lastItem;
}

// create a function to access the last element of an array.
// to access the last element of an array, it should be [array.length - 1];
// it should be written as array[i] where i is substituted as array.length - 1;
// array[array.length - 1];
